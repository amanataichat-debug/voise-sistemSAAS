; Voksy AI SIP gateway — RTP media ports.
; Must match the Hetzner firewall rule "RTP O!" (UDP 10000-20000) and the
; port range sent to the operator.
[general]
rtpstart=10000
rtpend=20000
strictrtp=yes
icesupport=no
